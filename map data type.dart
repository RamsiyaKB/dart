void main()
{
  Map<String,String>student={'a':'ss','b':'dd'};
  
  //find the number of key/value pairs using length
  
   print(student.length);
  
  //check if a Map is empty or not
  
  print(student.isEmpty);
  print(student.isNotEmpty);
  
  //get all keys or values with keys & values property.
  
  print(student.keys);
  print(student.values);
  
  //get value of specified key in a Map or operator []. 
  
  print(student['a']);
  
  
  
  }
